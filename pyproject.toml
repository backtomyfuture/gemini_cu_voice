[project]
name = "gemini-cu-voice"
version = "0.1.0"
description = "Gemini Live + kimi-cu + Ego Lite voice computer use"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "google-genai>=2.20.0",
    "mcp>=1.0.0",
    "sounddevice>=0.5.0",
    "numpy>=1.24.0",
    "python-dotenv>=1.0.0",
]
